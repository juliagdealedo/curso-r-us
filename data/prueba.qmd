---
title: "prueba"
format: html
editor: visual
---

## Cosas que he aprendido hoy

un montonodnoisdfnnsk
sdkfjsd
me voy super contento

## Ejemplos

Seleccionamos las columnas de y el modelo tiene que se rbinoa oejfoifwf f

```{r}

# Creamos vectores con información
nombres <-  c ("Laura", "Rocio", "Violeta")
edad <- c(23, 35, 29)
altura <- c(1.65, 1.80, 1.70)
profesion <- c("Bióloga", "Informática", "Médica")

# Combinamos todo en un data frame
personas <- data.frame(
  NAME = nombres,
  AGE = edad,
  HEIGHT = altura,
  JOB = profesion)

personas

```

You can add options to executable code like this

```{r}
#| echo: false

x <- rnorm(100)
hist(x)
```

The `echo: false` option disables the printing of code (only output is displayed).
